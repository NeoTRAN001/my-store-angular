import { StringAsciiValuePipe } from './string-ascii-value.pipe';

describe('StringAsciiValuePipe', () => {
  it('create an instance', () => {
    const pipe = new StringAsciiValuePipe();
    expect(pipe).toBeTruthy();
  });
});
